language: php
php:
  - 5.3
env:
  - DB=mysql
before_script:
  - mysql -e "create database IF NOT EXISTS ss_official;" -uroot
  - mysql -e "GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost';" -uroot
  - composer update
  - cd tests
script: phpunit -v